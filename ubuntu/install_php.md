# PHP7系のインストールを行う

```
$ sudo apt install software-properties-common
$ sudo apt-add-repository ppa:ondrej/php
$ sudo apt update
$ sudo apt show php7.3 # ←PHPのバージョンの確認
$ sudo apt install php7.3
$ php --version
```